apiVersion: v1
kind: Service
metadata:
  name: visualize-service
  labels:
    app: visualize
  namespace: myapps
spec:
  type: LoadBalancer
  ports:
  - port: 5003
    targetPort: 5000
  selector:
    app: predict